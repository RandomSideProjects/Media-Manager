{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the maintenance window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
