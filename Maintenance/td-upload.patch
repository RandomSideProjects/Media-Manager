--- a/src/upload.ts
+++ b/src/upload.ts
@@ -5,10 +5,10 @@
 import type { Session } from "./auth.ts";
 
 const CHUNK_SIZE = 32 * 1024 * 1024; // 32 MiB
-const MAX_RETRIES = 3;
+const MAX_RETRIES = 8;
 /** How many chunks to keep in flight simultaneously.
  *  With 2, chunk N+1 is being read/transferred/encrypted while chunk N
  *  is uploading to HF, overlapping the ~5 s upload gap. */
-const PIPELINE_CONCURRENCY = 2;
+const PIPELINE_CONCURRENCY = 1;
